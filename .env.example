VITE_DATA_SOURCE=mock

# ─── ETL real raw ingest API keys (사용자 로컬 전용, commit 금지) ───
# 본 행들은 키 이름만 자리표시. 실제 값은 사용자가 `.env.local`(gitignored)에
# 직접 입력. Claude Code / CI / Vercel은 본 키들을 사용·읽기·로그 출력하지 않는다.
# `VITE_*` prefix는 Vite client bundle에 노출되므로 인증키 prefix로 사용 금지
# (CLAUDE.md §10). `ETL_API_KEY_*` prefix만 사용한다.

# 11-3 1차-75 — NEIS schoolInfo OpenAPI 인증키 자리표시.
# 발급: 공공데이터포털(https://www.data.go.kr/) — 사용자 본인 계정으로 별도 신청.
# 사용처: scripts/etl/ 경로의 Node 실행 전용 (frontend 직접 호출 금지).
# 1차-75 scaffold 단계에서는 parser만 추가되어 본 키 의존도 0. 후속 사용자 manual
# fetch 단계 진입 시에만 사용한다.
ETL_API_KEY_NEIS=
